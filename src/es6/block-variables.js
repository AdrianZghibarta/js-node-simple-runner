const firstName = "George";

if (firstName === "George") {
    function printHello() {console.log("Hello " + firstName);}

    var lastName = "Bush";// let / const
}

console.log(`${firstName} ${lastName}`);
printHello(); // Call this ! :)